# ----------------------------------------------------------
# TAX CONSULTANTS
# Docker Compose Production Configuration
#
# This file is intended for production deployments.
# ----------------------------------------------------------

version: "3.9"

services:
  web:
    container_name: tax-consultants-prod

    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "3000:3000"

    env_file:
      - .env.local

    restart: always